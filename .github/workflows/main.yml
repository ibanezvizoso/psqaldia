name: Actualizar Boletín Semanal

on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # Subimos a 3.10 para evitar avisos de EOL
          
      - name: Instalar librerías
        run: |
          python -m pip install --upgrade pip
          pip install -U google-genai requests # Usamos la librería moderna
          
      - name: Ejecutar script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python update_boletin.py
          
      - name: Guardar cambios en GitHub
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add boletin.json
          git commit -m "Actualización automática: Boletín experimental" || exit 0
          git push
